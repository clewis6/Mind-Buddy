# Mind Buddy

Local Next.js + TypeScript MVP.

Run:

1. npm install
2. npm run dev

Open http://localhost:3000

Notes:
- Mock AI mode is default. To enable OpenAI mode set OPENAI_API_KEY in env.
- This is a demo; do not enter PHI.
